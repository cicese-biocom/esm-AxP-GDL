version: '3'

services:
  samppred-gat-esm2-env:
    build: .
    ports:
      - "5000:5000"
    command: nvidia-smi
    volumes:
      - .:/opt/project
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]